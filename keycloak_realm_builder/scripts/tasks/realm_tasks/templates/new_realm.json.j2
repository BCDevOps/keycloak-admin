{
  "id": "{{ realm_data.realm.id }}",
  "realm": "{{ realm_data.realm.id }}",
  "displayName":"{{ realm_data.realm.displayName }}",
  "displayNameHtml":"<a>{{ realm_data.realm.displayName }}</a>",
  "enabled": true,
  {% if custom %}
  "loginTheme": "bcgov",
  "duplicateEmailsAllowed": true,
  {% endif %}
  "groups":[
    {
      "name": "Realm Administrator",
      "path": "/Realm Administrator",
      "clientRoles": {
        "realm-management": [
          "realm-admin",
          "manage-identity-providers",
          "query-realms",
          "query-groups",
          "manage-authorization",
          {% if env != "prod" %}
          "impersonation",
          {% endif %}
          "query-clients",
          "manage-realm",
          "manage-clients",
          "query-users",
          "create-client",
          "manage-events",
          "manage-users"
        ]
      }
    }
  ]
}